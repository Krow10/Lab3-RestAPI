<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_page_start">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/user_settings_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="?attr/homeAsUpIndicator"
        android:background="@android:color/transparent"
        android:theme="@style/Widget.MaterialComponents.Toolbar.Transparent"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:titleTextColor="@color/white"/>

    <View
        android:id="@+id/user_settings_toolbar_shadow"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        app:layout_constraintTop_toBottomOf="@id/user_settings_toolbar"
        android:background="@drawable/bg_appbar_shadow"/>

    <FrameLayout
        android:id="@+id/user_settings_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/user_settings_toolbar_shadow"/>
</androidx.constraintlayout.widget.ConstraintLayout>